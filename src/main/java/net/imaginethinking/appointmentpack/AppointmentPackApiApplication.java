package net.imaginethinking.appointmentpack;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Starts the Appointment Pack application.
 */
@SpringBootApplication
public class AppointmentPackApiApplication {

    /**
     * Starts the Spring application using the supplied command line arguments.
     */
    public static void main(String[] args) {
        SpringApplication.run(AppointmentPackApiApplication.class, args);
    }

}
